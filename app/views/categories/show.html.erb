<div class="row text-color-primary">
  <div class="col-md-12">
    <h3><%= @category.name.capitalize %></h3>
    <%= render partial: 'photos/photo', collection: @category.photos, as: :photo %>
  </div>
</div>